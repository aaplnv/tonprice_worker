// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"main/ent/predicate"
	"main/ent/sekquote"
	"time"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// SEKQuoteUpdate is the builder for updating SEKQuote entities.
type SEKQuoteUpdate struct {
	config
	hooks    []Hook
	mutation *SEKQuoteMutation
}

// Where appends a list predicates to the SEKQuoteUpdate builder.
func (squ *SEKQuoteUpdate) Where(ps ...predicate.SEKQuote) *SEKQuoteUpdate {
	squ.mutation.Where(ps...)
	return squ
}

// SetPrice sets the "price" field.
func (squ *SEKQuoteUpdate) SetPrice(f float64) *SEKQuoteUpdate {
	squ.mutation.ResetPrice()
	squ.mutation.SetPrice(f)
	return squ
}

// AddPrice adds f to the "price" field.
func (squ *SEKQuoteUpdate) AddPrice(f float64) *SEKQuoteUpdate {
	squ.mutation.AddPrice(f)
	return squ
}

// SetTimestamp sets the "Timestamp" field.
func (squ *SEKQuoteUpdate) SetTimestamp(t time.Time) *SEKQuoteUpdate {
	squ.mutation.SetTimestamp(t)
	return squ
}

// Mutation returns the SEKQuoteMutation object of the builder.
func (squ *SEKQuoteUpdate) Mutation() *SEKQuoteMutation {
	return squ.mutation
}

// Save executes the query and returns the number of nodes affected by the update operation.
func (squ *SEKQuoteUpdate) Save(ctx context.Context) (int, error) {
	var (
		err      error
		affected int
	)
	if len(squ.hooks) == 0 {
		affected, err = squ.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*SEKQuoteMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			squ.mutation = mutation
			affected, err = squ.sqlSave(ctx)
			mutation.done = true
			return affected, err
		})
		for i := len(squ.hooks) - 1; i >= 0; i-- {
			if squ.hooks[i] == nil {
				return 0, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = squ.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, squ.mutation); err != nil {
			return 0, err
		}
	}
	return affected, err
}

// SaveX is like Save, but panics if an error occurs.
func (squ *SEKQuoteUpdate) SaveX(ctx context.Context) int {
	affected, err := squ.Save(ctx)
	if err != nil {
		panic(err)
	}
	return affected
}

// Exec executes the query.
func (squ *SEKQuoteUpdate) Exec(ctx context.Context) error {
	_, err := squ.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (squ *SEKQuoteUpdate) ExecX(ctx context.Context) {
	if err := squ.Exec(ctx); err != nil {
		panic(err)
	}
}

func (squ *SEKQuoteUpdate) sqlSave(ctx context.Context) (n int, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   sekquote.Table,
			Columns: sekquote.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: sekquote.FieldID,
			},
		},
	}
	if ps := squ.mutation.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := squ.mutation.Price(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeFloat64,
			Value:  value,
			Column: sekquote.FieldPrice,
		})
	}
	if value, ok := squ.mutation.AddedPrice(); ok {
		_spec.Fields.Add = append(_spec.Fields.Add, &sqlgraph.FieldSpec{
			Type:   field.TypeFloat64,
			Value:  value,
			Column: sekquote.FieldPrice,
		})
	}
	if value, ok := squ.mutation.Timestamp(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: sekquote.FieldTimestamp,
		})
	}
	if n, err = sqlgraph.UpdateNodes(ctx, squ.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{sekquote.Label}
		} else if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return 0, err
	}
	return n, nil
}

// SEKQuoteUpdateOne is the builder for updating a single SEKQuote entity.
type SEKQuoteUpdateOne struct {
	config
	fields   []string
	hooks    []Hook
	mutation *SEKQuoteMutation
}

// SetPrice sets the "price" field.
func (squo *SEKQuoteUpdateOne) SetPrice(f float64) *SEKQuoteUpdateOne {
	squo.mutation.ResetPrice()
	squo.mutation.SetPrice(f)
	return squo
}

// AddPrice adds f to the "price" field.
func (squo *SEKQuoteUpdateOne) AddPrice(f float64) *SEKQuoteUpdateOne {
	squo.mutation.AddPrice(f)
	return squo
}

// SetTimestamp sets the "Timestamp" field.
func (squo *SEKQuoteUpdateOne) SetTimestamp(t time.Time) *SEKQuoteUpdateOne {
	squo.mutation.SetTimestamp(t)
	return squo
}

// Mutation returns the SEKQuoteMutation object of the builder.
func (squo *SEKQuoteUpdateOne) Mutation() *SEKQuoteMutation {
	return squo.mutation
}

// Select allows selecting one or more fields (columns) of the returned entity.
// The default is selecting all fields defined in the entity schema.
func (squo *SEKQuoteUpdateOne) Select(field string, fields ...string) *SEKQuoteUpdateOne {
	squo.fields = append([]string{field}, fields...)
	return squo
}

// Save executes the query and returns the updated SEKQuote entity.
func (squo *SEKQuoteUpdateOne) Save(ctx context.Context) (*SEKQuote, error) {
	var (
		err  error
		node *SEKQuote
	)
	if len(squo.hooks) == 0 {
		node, err = squo.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*SEKQuoteMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			squo.mutation = mutation
			node, err = squo.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(squo.hooks) - 1; i >= 0; i-- {
			if squo.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = squo.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, squo.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX is like Save, but panics if an error occurs.
func (squo *SEKQuoteUpdateOne) SaveX(ctx context.Context) *SEKQuote {
	node, err := squo.Save(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// Exec executes the query on the entity.
func (squo *SEKQuoteUpdateOne) Exec(ctx context.Context) error {
	_, err := squo.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (squo *SEKQuoteUpdateOne) ExecX(ctx context.Context) {
	if err := squo.Exec(ctx); err != nil {
		panic(err)
	}
}

func (squo *SEKQuoteUpdateOne) sqlSave(ctx context.Context) (_node *SEKQuote, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   sekquote.Table,
			Columns: sekquote.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: sekquote.FieldID,
			},
		},
	}
	id, ok := squo.mutation.ID()
	if !ok {
		return nil, &ValidationError{Name: "id", err: errors.New(`ent: missing "SEKQuote.id" for update`)}
	}
	_spec.Node.ID.Value = id
	if fields := squo.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, sekquote.FieldID)
		for _, f := range fields {
			if !sekquote.ValidColumn(f) {
				return nil, &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
			}
			if f != sekquote.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, f)
			}
		}
	}
	if ps := squo.mutation.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := squo.mutation.Price(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeFloat64,
			Value:  value,
			Column: sekquote.FieldPrice,
		})
	}
	if value, ok := squo.mutation.AddedPrice(); ok {
		_spec.Fields.Add = append(_spec.Fields.Add, &sqlgraph.FieldSpec{
			Type:   field.TypeFloat64,
			Value:  value,
			Column: sekquote.FieldPrice,
		})
	}
	if value, ok := squo.mutation.Timestamp(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: sekquote.FieldTimestamp,
		})
	}
	_node = &SEKQuote{config: squo.config}
	_spec.Assign = _node.assignValues
	_spec.ScanValues = _node.scanValues
	if err = sqlgraph.UpdateNode(ctx, squo.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{sekquote.Label}
		} else if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	return _node, nil
}
