// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"main/ent/hkdquote"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
)

// HKDQuote is the model entity for the HKDQuote schema.
type HKDQuote struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Price holds the value of the "price" field.
	Price float64 `json:"price,omitempty"`
	// Timestamp holds the value of the "Timestamp" field.
	Timestamp time.Time `json:"Timestamp,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*HKDQuote) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case hkdquote.FieldPrice:
			values[i] = new(sql.NullFloat64)
		case hkdquote.FieldID:
			values[i] = new(sql.NullInt64)
		case hkdquote.FieldTimestamp:
			values[i] = new(sql.NullTime)
		default:
			return nil, fmt.Errorf("unexpected column %q for type HKDQuote", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the HKDQuote fields.
func (hq *HKDQuote) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case hkdquote.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			hq.ID = int(value.Int64)
		case hkdquote.FieldPrice:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field price", values[i])
			} else if value.Valid {
				hq.Price = value.Float64
			}
		case hkdquote.FieldTimestamp:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field Timestamp", values[i])
			} else if value.Valid {
				hq.Timestamp = value.Time
			}
		}
	}
	return nil
}

// Update returns a builder for updating this HKDQuote.
// Note that you need to call HKDQuote.Unwrap() before calling this method if this HKDQuote
// was returned from a transaction, and the transaction was committed or rolled back.
func (hq *HKDQuote) Update() *HKDQuoteUpdateOne {
	return (&HKDQuoteClient{config: hq.config}).UpdateOne(hq)
}

// Unwrap unwraps the HKDQuote entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (hq *HKDQuote) Unwrap() *HKDQuote {
	tx, ok := hq.config.driver.(*txDriver)
	if !ok {
		panic("ent: HKDQuote is not a transactional entity")
	}
	hq.config.driver = tx.drv
	return hq
}

// String implements the fmt.Stringer.
func (hq *HKDQuote) String() string {
	var builder strings.Builder
	builder.WriteString("HKDQuote(")
	builder.WriteString(fmt.Sprintf("id=%v", hq.ID))
	builder.WriteString(", price=")
	builder.WriteString(fmt.Sprintf("%v", hq.Price))
	builder.WriteString(", Timestamp=")
	builder.WriteString(hq.Timestamp.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// HKDQuotes is a parsable slice of HKDQuote.
type HKDQuotes []*HKDQuote

func (hq HKDQuotes) config(cfg config) {
	for _i := range hq {
		hq[_i].config = cfg
	}
}
