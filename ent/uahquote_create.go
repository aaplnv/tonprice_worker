// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"main/ent/uahquote"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UAHQuoteCreate is the builder for creating a UAHQuote entity.
type UAHQuoteCreate struct {
	config
	mutation *UAHQuoteMutation
	hooks    []Hook
}

// SetPrice sets the "price" field.
func (uqc *UAHQuoteCreate) SetPrice(f float64) *UAHQuoteCreate {
	uqc.mutation.SetPrice(f)
	return uqc
}

// SetTimestamp sets the "Timestamp" field.
func (uqc *UAHQuoteCreate) SetTimestamp(t time.Time) *UAHQuoteCreate {
	uqc.mutation.SetTimestamp(t)
	return uqc
}

// SetID sets the "id" field.
func (uqc *UAHQuoteCreate) SetID(i int) *UAHQuoteCreate {
	uqc.mutation.SetID(i)
	return uqc
}

// Mutation returns the UAHQuoteMutation object of the builder.
func (uqc *UAHQuoteCreate) Mutation() *UAHQuoteMutation {
	return uqc.mutation
}

// Save creates the UAHQuote in the database.
func (uqc *UAHQuoteCreate) Save(ctx context.Context) (*UAHQuote, error) {
	var (
		err  error
		node *UAHQuote
	)
	if len(uqc.hooks) == 0 {
		if err = uqc.check(); err != nil {
			return nil, err
		}
		node, err = uqc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*UAHQuoteMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = uqc.check(); err != nil {
				return nil, err
			}
			uqc.mutation = mutation
			if node, err = uqc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(uqc.hooks) - 1; i >= 0; i-- {
			if uqc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = uqc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, uqc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (uqc *UAHQuoteCreate) SaveX(ctx context.Context) *UAHQuote {
	v, err := uqc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (uqc *UAHQuoteCreate) Exec(ctx context.Context) error {
	_, err := uqc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (uqc *UAHQuoteCreate) ExecX(ctx context.Context) {
	if err := uqc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (uqc *UAHQuoteCreate) check() error {
	if _, ok := uqc.mutation.Price(); !ok {
		return &ValidationError{Name: "price", err: errors.New(`ent: missing required field "UAHQuote.price"`)}
	}
	if _, ok := uqc.mutation.Timestamp(); !ok {
		return &ValidationError{Name: "Timestamp", err: errors.New(`ent: missing required field "UAHQuote.Timestamp"`)}
	}
	return nil
}

func (uqc *UAHQuoteCreate) sqlSave(ctx context.Context) (*UAHQuote, error) {
	_node, _spec := uqc.createSpec()
	if err := sqlgraph.CreateNode(ctx, uqc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	if _spec.ID.Value != _node.ID {
		id := _spec.ID.Value.(int64)
		_node.ID = int(id)
	}
	return _node, nil
}

func (uqc *UAHQuoteCreate) createSpec() (*UAHQuote, *sqlgraph.CreateSpec) {
	var (
		_node = &UAHQuote{config: uqc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: uahquote.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: uahquote.FieldID,
			},
		}
	)
	if id, ok := uqc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := uqc.mutation.Price(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeFloat64,
			Value:  value,
			Column: uahquote.FieldPrice,
		})
		_node.Price = value
	}
	if value, ok := uqc.mutation.Timestamp(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: uahquote.FieldTimestamp,
		})
		_node.Timestamp = value
	}
	return _node, _spec
}

// UAHQuoteCreateBulk is the builder for creating many UAHQuote entities in bulk.
type UAHQuoteCreateBulk struct {
	config
	builders []*UAHQuoteCreate
}

// Save creates the UAHQuote entities in the database.
func (uqcb *UAHQuoteCreateBulk) Save(ctx context.Context) ([]*UAHQuote, error) {
	specs := make([]*sqlgraph.CreateSpec, len(uqcb.builders))
	nodes := make([]*UAHQuote, len(uqcb.builders))
	mutators := make([]Mutator, len(uqcb.builders))
	for i := range uqcb.builders {
		func(i int, root context.Context) {
			builder := uqcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*UAHQuoteMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, uqcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, uqcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil && nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, uqcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (uqcb *UAHQuoteCreateBulk) SaveX(ctx context.Context) []*UAHQuote {
	v, err := uqcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (uqcb *UAHQuoteCreateBulk) Exec(ctx context.Context) error {
	_, err := uqcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (uqcb *UAHQuoteCreateBulk) ExecX(ctx context.Context) {
	if err := uqcb.Exec(ctx); err != nil {
		panic(err)
	}
}
