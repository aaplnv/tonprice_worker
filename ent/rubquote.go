// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"main/ent/rubquote"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
)

// RUBQuote is the model entity for the RUBQuote schema.
type RUBQuote struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Price holds the value of the "price" field.
	Price float64 `json:"price,omitempty"`
	// Timestamp holds the value of the "Timestamp" field.
	Timestamp time.Time `json:"Timestamp,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RUBQuote) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case rubquote.FieldPrice:
			values[i] = new(sql.NullFloat64)
		case rubquote.FieldID:
			values[i] = new(sql.NullInt64)
		case rubquote.FieldTimestamp:
			values[i] = new(sql.NullTime)
		default:
			return nil, fmt.Errorf("unexpected column %q for type RUBQuote", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RUBQuote fields.
func (rq *RUBQuote) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case rubquote.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			rq.ID = int(value.Int64)
		case rubquote.FieldPrice:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field price", values[i])
			} else if value.Valid {
				rq.Price = value.Float64
			}
		case rubquote.FieldTimestamp:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field Timestamp", values[i])
			} else if value.Valid {
				rq.Timestamp = value.Time
			}
		}
	}
	return nil
}

// Update returns a builder for updating this RUBQuote.
// Note that you need to call RUBQuote.Unwrap() before calling this method if this RUBQuote
// was returned from a transaction, and the transaction was committed or rolled back.
func (rq *RUBQuote) Update() *RUBQuoteUpdateOne {
	return (&RUBQuoteClient{config: rq.config}).UpdateOne(rq)
}

// Unwrap unwraps the RUBQuote entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (rq *RUBQuote) Unwrap() *RUBQuote {
	tx, ok := rq.config.driver.(*txDriver)
	if !ok {
		panic("ent: RUBQuote is not a transactional entity")
	}
	rq.config.driver = tx.drv
	return rq
}

// String implements the fmt.Stringer.
func (rq *RUBQuote) String() string {
	var builder strings.Builder
	builder.WriteString("RUBQuote(")
	builder.WriteString(fmt.Sprintf("id=%v", rq.ID))
	builder.WriteString(", price=")
	builder.WriteString(fmt.Sprintf("%v", rq.Price))
	builder.WriteString(", Timestamp=")
	builder.WriteString(rq.Timestamp.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// RUBQuotes is a parsable slice of RUBQuote.
type RUBQuotes []*RUBQuote

func (rq RUBQuotes) config(cfg config) {
	for _i := range rq {
		rq[_i].config = cfg
	}
}
