// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"main/ent/hkdquote"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// HKDQuoteCreate is the builder for creating a HKDQuote entity.
type HKDQuoteCreate struct {
	config
	mutation *HKDQuoteMutation
	hooks    []Hook
}

// SetPrice sets the "price" field.
func (hqc *HKDQuoteCreate) SetPrice(f float64) *HKDQuoteCreate {
	hqc.mutation.SetPrice(f)
	return hqc
}

// SetTimestamp sets the "Timestamp" field.
func (hqc *HKDQuoteCreate) SetTimestamp(t time.Time) *HKDQuoteCreate {
	hqc.mutation.SetTimestamp(t)
	return hqc
}

// SetID sets the "id" field.
func (hqc *HKDQuoteCreate) SetID(i int) *HKDQuoteCreate {
	hqc.mutation.SetID(i)
	return hqc
}

// Mutation returns the HKDQuoteMutation object of the builder.
func (hqc *HKDQuoteCreate) Mutation() *HKDQuoteMutation {
	return hqc.mutation
}

// Save creates the HKDQuote in the database.
func (hqc *HKDQuoteCreate) Save(ctx context.Context) (*HKDQuote, error) {
	var (
		err  error
		node *HKDQuote
	)
	if len(hqc.hooks) == 0 {
		if err = hqc.check(); err != nil {
			return nil, err
		}
		node, err = hqc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*HKDQuoteMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = hqc.check(); err != nil {
				return nil, err
			}
			hqc.mutation = mutation
			if node, err = hqc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(hqc.hooks) - 1; i >= 0; i-- {
			if hqc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = hqc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, hqc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (hqc *HKDQuoteCreate) SaveX(ctx context.Context) *HKDQuote {
	v, err := hqc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (hqc *HKDQuoteCreate) Exec(ctx context.Context) error {
	_, err := hqc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (hqc *HKDQuoteCreate) ExecX(ctx context.Context) {
	if err := hqc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (hqc *HKDQuoteCreate) check() error {
	if _, ok := hqc.mutation.Price(); !ok {
		return &ValidationError{Name: "price", err: errors.New(`ent: missing required field "HKDQuote.price"`)}
	}
	if _, ok := hqc.mutation.Timestamp(); !ok {
		return &ValidationError{Name: "Timestamp", err: errors.New(`ent: missing required field "HKDQuote.Timestamp"`)}
	}
	return nil
}

func (hqc *HKDQuoteCreate) sqlSave(ctx context.Context) (*HKDQuote, error) {
	_node, _spec := hqc.createSpec()
	if err := sqlgraph.CreateNode(ctx, hqc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	if _spec.ID.Value != _node.ID {
		id := _spec.ID.Value.(int64)
		_node.ID = int(id)
	}
	return _node, nil
}

func (hqc *HKDQuoteCreate) createSpec() (*HKDQuote, *sqlgraph.CreateSpec) {
	var (
		_node = &HKDQuote{config: hqc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: hkdquote.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: hkdquote.FieldID,
			},
		}
	)
	if id, ok := hqc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := hqc.mutation.Price(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeFloat64,
			Value:  value,
			Column: hkdquote.FieldPrice,
		})
		_node.Price = value
	}
	if value, ok := hqc.mutation.Timestamp(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: hkdquote.FieldTimestamp,
		})
		_node.Timestamp = value
	}
	return _node, _spec
}

// HKDQuoteCreateBulk is the builder for creating many HKDQuote entities in bulk.
type HKDQuoteCreateBulk struct {
	config
	builders []*HKDQuoteCreate
}

// Save creates the HKDQuote entities in the database.
func (hqcb *HKDQuoteCreateBulk) Save(ctx context.Context) ([]*HKDQuote, error) {
	specs := make([]*sqlgraph.CreateSpec, len(hqcb.builders))
	nodes := make([]*HKDQuote, len(hqcb.builders))
	mutators := make([]Mutator, len(hqcb.builders))
	for i := range hqcb.builders {
		func(i int, root context.Context) {
			builder := hqcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*HKDQuoteMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, hqcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, hqcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil && nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, hqcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (hqcb *HKDQuoteCreateBulk) SaveX(ctx context.Context) []*HKDQuote {
	v, err := hqcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (hqcb *HKDQuoteCreateBulk) Exec(ctx context.Context) error {
	_, err := hqcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (hqcb *HKDQuoteCreateBulk) ExecX(ctx context.Context) {
	if err := hqcb.Exec(ctx); err != nil {
		panic(err)
	}
}
