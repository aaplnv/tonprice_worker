// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"main/ent/rubquote"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// RUBQuoteCreate is the builder for creating a RUBQuote entity.
type RUBQuoteCreate struct {
	config
	mutation *RUBQuoteMutation
	hooks    []Hook
}

// SetPrice sets the "price" field.
func (rqc *RUBQuoteCreate) SetPrice(f float64) *RUBQuoteCreate {
	rqc.mutation.SetPrice(f)
	return rqc
}

// SetTimestamp sets the "Timestamp" field.
func (rqc *RUBQuoteCreate) SetTimestamp(t time.Time) *RUBQuoteCreate {
	rqc.mutation.SetTimestamp(t)
	return rqc
}

// SetID sets the "id" field.
func (rqc *RUBQuoteCreate) SetID(i int) *RUBQuoteCreate {
	rqc.mutation.SetID(i)
	return rqc
}

// Mutation returns the RUBQuoteMutation object of the builder.
func (rqc *RUBQuoteCreate) Mutation() *RUBQuoteMutation {
	return rqc.mutation
}

// Save creates the RUBQuote in the database.
func (rqc *RUBQuoteCreate) Save(ctx context.Context) (*RUBQuote, error) {
	var (
		err  error
		node *RUBQuote
	)
	if len(rqc.hooks) == 0 {
		if err = rqc.check(); err != nil {
			return nil, err
		}
		node, err = rqc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*RUBQuoteMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = rqc.check(); err != nil {
				return nil, err
			}
			rqc.mutation = mutation
			if node, err = rqc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(rqc.hooks) - 1; i >= 0; i-- {
			if rqc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = rqc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, rqc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (rqc *RUBQuoteCreate) SaveX(ctx context.Context) *RUBQuote {
	v, err := rqc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (rqc *RUBQuoteCreate) Exec(ctx context.Context) error {
	_, err := rqc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (rqc *RUBQuoteCreate) ExecX(ctx context.Context) {
	if err := rqc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (rqc *RUBQuoteCreate) check() error {
	if _, ok := rqc.mutation.Price(); !ok {
		return &ValidationError{Name: "price", err: errors.New(`ent: missing required field "RUBQuote.price"`)}
	}
	if _, ok := rqc.mutation.Timestamp(); !ok {
		return &ValidationError{Name: "Timestamp", err: errors.New(`ent: missing required field "RUBQuote.Timestamp"`)}
	}
	return nil
}

func (rqc *RUBQuoteCreate) sqlSave(ctx context.Context) (*RUBQuote, error) {
	_node, _spec := rqc.createSpec()
	if err := sqlgraph.CreateNode(ctx, rqc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	if _spec.ID.Value != _node.ID {
		id := _spec.ID.Value.(int64)
		_node.ID = int(id)
	}
	return _node, nil
}

func (rqc *RUBQuoteCreate) createSpec() (*RUBQuote, *sqlgraph.CreateSpec) {
	var (
		_node = &RUBQuote{config: rqc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: rubquote.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: rubquote.FieldID,
			},
		}
	)
	if id, ok := rqc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := rqc.mutation.Price(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeFloat64,
			Value:  value,
			Column: rubquote.FieldPrice,
		})
		_node.Price = value
	}
	if value, ok := rqc.mutation.Timestamp(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: rubquote.FieldTimestamp,
		})
		_node.Timestamp = value
	}
	return _node, _spec
}

// RUBQuoteCreateBulk is the builder for creating many RUBQuote entities in bulk.
type RUBQuoteCreateBulk struct {
	config
	builders []*RUBQuoteCreate
}

// Save creates the RUBQuote entities in the database.
func (rqcb *RUBQuoteCreateBulk) Save(ctx context.Context) ([]*RUBQuote, error) {
	specs := make([]*sqlgraph.CreateSpec, len(rqcb.builders))
	nodes := make([]*RUBQuote, len(rqcb.builders))
	mutators := make([]Mutator, len(rqcb.builders))
	for i := range rqcb.builders {
		func(i int, root context.Context) {
			builder := rqcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*RUBQuoteMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, rqcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, rqcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil && nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, rqcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (rqcb *RUBQuoteCreateBulk) SaveX(ctx context.Context) []*RUBQuote {
	v, err := rqcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (rqcb *RUBQuoteCreateBulk) Exec(ctx context.Context) error {
	_, err := rqcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (rqcb *RUBQuoteCreateBulk) ExecX(ctx context.Context) {
	if err := rqcb.Exec(ctx); err != nil {
		panic(err)
	}
}
