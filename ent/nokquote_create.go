// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"main/ent/nokquote"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// NOKQuoteCreate is the builder for creating a NOKQuote entity.
type NOKQuoteCreate struct {
	config
	mutation *NOKQuoteMutation
	hooks    []Hook
}

// SetPrice sets the "price" field.
func (nqc *NOKQuoteCreate) SetPrice(f float64) *NOKQuoteCreate {
	nqc.mutation.SetPrice(f)
	return nqc
}

// SetTimestamp sets the "Timestamp" field.
func (nqc *NOKQuoteCreate) SetTimestamp(t time.Time) *NOKQuoteCreate {
	nqc.mutation.SetTimestamp(t)
	return nqc
}

// SetID sets the "id" field.
func (nqc *NOKQuoteCreate) SetID(i int) *NOKQuoteCreate {
	nqc.mutation.SetID(i)
	return nqc
}

// Mutation returns the NOKQuoteMutation object of the builder.
func (nqc *NOKQuoteCreate) Mutation() *NOKQuoteMutation {
	return nqc.mutation
}

// Save creates the NOKQuote in the database.
func (nqc *NOKQuoteCreate) Save(ctx context.Context) (*NOKQuote, error) {
	var (
		err  error
		node *NOKQuote
	)
	if len(nqc.hooks) == 0 {
		if err = nqc.check(); err != nil {
			return nil, err
		}
		node, err = nqc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*NOKQuoteMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = nqc.check(); err != nil {
				return nil, err
			}
			nqc.mutation = mutation
			if node, err = nqc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(nqc.hooks) - 1; i >= 0; i-- {
			if nqc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = nqc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, nqc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (nqc *NOKQuoteCreate) SaveX(ctx context.Context) *NOKQuote {
	v, err := nqc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (nqc *NOKQuoteCreate) Exec(ctx context.Context) error {
	_, err := nqc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (nqc *NOKQuoteCreate) ExecX(ctx context.Context) {
	if err := nqc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (nqc *NOKQuoteCreate) check() error {
	if _, ok := nqc.mutation.Price(); !ok {
		return &ValidationError{Name: "price", err: errors.New(`ent: missing required field "NOKQuote.price"`)}
	}
	if _, ok := nqc.mutation.Timestamp(); !ok {
		return &ValidationError{Name: "Timestamp", err: errors.New(`ent: missing required field "NOKQuote.Timestamp"`)}
	}
	return nil
}

func (nqc *NOKQuoteCreate) sqlSave(ctx context.Context) (*NOKQuote, error) {
	_node, _spec := nqc.createSpec()
	if err := sqlgraph.CreateNode(ctx, nqc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	if _spec.ID.Value != _node.ID {
		id := _spec.ID.Value.(int64)
		_node.ID = int(id)
	}
	return _node, nil
}

func (nqc *NOKQuoteCreate) createSpec() (*NOKQuote, *sqlgraph.CreateSpec) {
	var (
		_node = &NOKQuote{config: nqc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: nokquote.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: nokquote.FieldID,
			},
		}
	)
	if id, ok := nqc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := nqc.mutation.Price(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeFloat64,
			Value:  value,
			Column: nokquote.FieldPrice,
		})
		_node.Price = value
	}
	if value, ok := nqc.mutation.Timestamp(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: nokquote.FieldTimestamp,
		})
		_node.Timestamp = value
	}
	return _node, _spec
}

// NOKQuoteCreateBulk is the builder for creating many NOKQuote entities in bulk.
type NOKQuoteCreateBulk struct {
	config
	builders []*NOKQuoteCreate
}

// Save creates the NOKQuote entities in the database.
func (nqcb *NOKQuoteCreateBulk) Save(ctx context.Context) ([]*NOKQuote, error) {
	specs := make([]*sqlgraph.CreateSpec, len(nqcb.builders))
	nodes := make([]*NOKQuote, len(nqcb.builders))
	mutators := make([]Mutator, len(nqcb.builders))
	for i := range nqcb.builders {
		func(i int, root context.Context) {
			builder := nqcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*NOKQuoteMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, nqcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, nqcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil && nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, nqcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (nqcb *NOKQuoteCreateBulk) SaveX(ctx context.Context) []*NOKQuote {
	v, err := nqcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (nqcb *NOKQuoteCreateBulk) Exec(ctx context.Context) error {
	_, err := nqcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (nqcb *NOKQuoteCreateBulk) ExecX(ctx context.Context) {
	if err := nqcb.Exec(ctx); err != nil {
		panic(err)
	}
}
