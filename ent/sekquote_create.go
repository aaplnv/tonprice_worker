// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"main/ent/sekquote"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// SEKQuoteCreate is the builder for creating a SEKQuote entity.
type SEKQuoteCreate struct {
	config
	mutation *SEKQuoteMutation
	hooks    []Hook
}

// SetPrice sets the "price" field.
func (sqc *SEKQuoteCreate) SetPrice(f float64) *SEKQuoteCreate {
	sqc.mutation.SetPrice(f)
	return sqc
}

// SetTimestamp sets the "Timestamp" field.
func (sqc *SEKQuoteCreate) SetTimestamp(t time.Time) *SEKQuoteCreate {
	sqc.mutation.SetTimestamp(t)
	return sqc
}

// SetID sets the "id" field.
func (sqc *SEKQuoteCreate) SetID(i int) *SEKQuoteCreate {
	sqc.mutation.SetID(i)
	return sqc
}

// Mutation returns the SEKQuoteMutation object of the builder.
func (sqc *SEKQuoteCreate) Mutation() *SEKQuoteMutation {
	return sqc.mutation
}

// Save creates the SEKQuote in the database.
func (sqc *SEKQuoteCreate) Save(ctx context.Context) (*SEKQuote, error) {
	var (
		err  error
		node *SEKQuote
	)
	if len(sqc.hooks) == 0 {
		if err = sqc.check(); err != nil {
			return nil, err
		}
		node, err = sqc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*SEKQuoteMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = sqc.check(); err != nil {
				return nil, err
			}
			sqc.mutation = mutation
			if node, err = sqc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(sqc.hooks) - 1; i >= 0; i-- {
			if sqc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = sqc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, sqc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (sqc *SEKQuoteCreate) SaveX(ctx context.Context) *SEKQuote {
	v, err := sqc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (sqc *SEKQuoteCreate) Exec(ctx context.Context) error {
	_, err := sqc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (sqc *SEKQuoteCreate) ExecX(ctx context.Context) {
	if err := sqc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (sqc *SEKQuoteCreate) check() error {
	if _, ok := sqc.mutation.Price(); !ok {
		return &ValidationError{Name: "price", err: errors.New(`ent: missing required field "SEKQuote.price"`)}
	}
	if _, ok := sqc.mutation.Timestamp(); !ok {
		return &ValidationError{Name: "Timestamp", err: errors.New(`ent: missing required field "SEKQuote.Timestamp"`)}
	}
	return nil
}

func (sqc *SEKQuoteCreate) sqlSave(ctx context.Context) (*SEKQuote, error) {
	_node, _spec := sqc.createSpec()
	if err := sqlgraph.CreateNode(ctx, sqc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	if _spec.ID.Value != _node.ID {
		id := _spec.ID.Value.(int64)
		_node.ID = int(id)
	}
	return _node, nil
}

func (sqc *SEKQuoteCreate) createSpec() (*SEKQuote, *sqlgraph.CreateSpec) {
	var (
		_node = &SEKQuote{config: sqc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: sekquote.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: sekquote.FieldID,
			},
		}
	)
	if id, ok := sqc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := sqc.mutation.Price(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeFloat64,
			Value:  value,
			Column: sekquote.FieldPrice,
		})
		_node.Price = value
	}
	if value, ok := sqc.mutation.Timestamp(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: sekquote.FieldTimestamp,
		})
		_node.Timestamp = value
	}
	return _node, _spec
}

// SEKQuoteCreateBulk is the builder for creating many SEKQuote entities in bulk.
type SEKQuoteCreateBulk struct {
	config
	builders []*SEKQuoteCreate
}

// Save creates the SEKQuote entities in the database.
func (sqcb *SEKQuoteCreateBulk) Save(ctx context.Context) ([]*SEKQuote, error) {
	specs := make([]*sqlgraph.CreateSpec, len(sqcb.builders))
	nodes := make([]*SEKQuote, len(sqcb.builders))
	mutators := make([]Mutator, len(sqcb.builders))
	for i := range sqcb.builders {
		func(i int, root context.Context) {
			builder := sqcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*SEKQuoteMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, sqcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, sqcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil && nodes[i].ID == 0 {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, sqcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (sqcb *SEKQuoteCreateBulk) SaveX(ctx context.Context) []*SEKQuote {
	v, err := sqcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (sqcb *SEKQuoteCreateBulk) Exec(ctx context.Context) error {
	_, err := sqcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (sqcb *SEKQuoteCreateBulk) ExecX(ctx context.Context) {
	if err := sqcb.Exec(ctx); err != nil {
		panic(err)
	}
}
