// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"main/ent/jpyquote"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
)

// JPYQuote is the model entity for the JPYQuote schema.
type JPYQuote struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Price holds the value of the "price" field.
	Price float64 `json:"price,omitempty"`
	// Timestamp holds the value of the "Timestamp" field.
	Timestamp time.Time `json:"Timestamp,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*JPYQuote) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case jpyquote.FieldPrice:
			values[i] = new(sql.NullFloat64)
		case jpyquote.FieldID:
			values[i] = new(sql.NullInt64)
		case jpyquote.FieldTimestamp:
			values[i] = new(sql.NullTime)
		default:
			return nil, fmt.Errorf("unexpected column %q for type JPYQuote", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the JPYQuote fields.
func (jq *JPYQuote) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case jpyquote.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			jq.ID = int(value.Int64)
		case jpyquote.FieldPrice:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field price", values[i])
			} else if value.Valid {
				jq.Price = value.Float64
			}
		case jpyquote.FieldTimestamp:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field Timestamp", values[i])
			} else if value.Valid {
				jq.Timestamp = value.Time
			}
		}
	}
	return nil
}

// Update returns a builder for updating this JPYQuote.
// Note that you need to call JPYQuote.Unwrap() before calling this method if this JPYQuote
// was returned from a transaction, and the transaction was committed or rolled back.
func (jq *JPYQuote) Update() *JPYQuoteUpdateOne {
	return (&JPYQuoteClient{config: jq.config}).UpdateOne(jq)
}

// Unwrap unwraps the JPYQuote entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (jq *JPYQuote) Unwrap() *JPYQuote {
	tx, ok := jq.config.driver.(*txDriver)
	if !ok {
		panic("ent: JPYQuote is not a transactional entity")
	}
	jq.config.driver = tx.drv
	return jq
}

// String implements the fmt.Stringer.
func (jq *JPYQuote) String() string {
	var builder strings.Builder
	builder.WriteString("JPYQuote(")
	builder.WriteString(fmt.Sprintf("id=%v", jq.ID))
	builder.WriteString(", price=")
	builder.WriteString(fmt.Sprintf("%v", jq.Price))
	builder.WriteString(", Timestamp=")
	builder.WriteString(jq.Timestamp.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// JPYQuotes is a parsable slice of JPYQuote.
type JPYQuotes []*JPYQuote

func (jq JPYQuotes) config(cfg config) {
	for _i := range jq {
		jq[_i].config = cfg
	}
}
